[project]
name = "sovereign-agent-advanced-patterns"
version = "0.1.0"
description = "Six offline failure-boundary labs for Sovereign Agent 1.3"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sovereign-agent==1.3.0",
]

[tool.uv]
package = false

[tool.sovereign-catalog]
smoke = "python check_examples.py"
