[project]
name = "zeocore-0100"
version = "0.1.0"
description = "Offline marketing, managed environments, service profiles and authoring receipts"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zeocore[jupytext,notebook,pandoc]==0.10.0",
]

[tool.uv]
package = false

[tool.sovereign-catalog]
smoke = "python src/main.py"
