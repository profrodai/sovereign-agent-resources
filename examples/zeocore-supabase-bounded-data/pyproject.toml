[project]
name = "zeocore-supabase-bounded-data"
version = "0.1.0"
description = "Offline proof of Zeocore's bounded Supabase database integration surface"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zeocore[supabase]==0.9.0",
]

[tool.uv]
package = false

[tool.sovereign-catalog]
smoke = "python src/main.py"
