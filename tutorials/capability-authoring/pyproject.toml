[project]
name = "capability-authoring"
version = "0.1.0"
description = "Build, register, and invoke a typed zeocore capability with no credentials"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zeocore==0.6.0",
]

[tool.sovereign-catalog]
smoke = "python src/main.py"
